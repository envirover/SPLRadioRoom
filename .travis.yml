language: cpp

before_script:

script: 
  - mkdir build
  - cd build
  - cmake -G "Unix Makefiles" ..
  - cmake --build .
  - cd ../bin
  - ls 
  - cpack -G DEB --debug -V
  - cat _CPack_Packages/Linux/DEB/PreinstallOutput.log